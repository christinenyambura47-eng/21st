
{
  "version": 3,
  "builds": [
    {
      "src": "twentyfirst-connect/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "(.*)", "dest": "/index.html" }
  ]
}
